import org.json.simple.parser.ParseException;

import javafx.application.Application;

public class Main{
	
	public static void main(String[] args) throws ParseException {
		Application.launch(FxFrame.class, args);
		/*
		 * ////////////// //print route ////////////// PathList path = new PathList();
		 * Scanner input = new Scanner(System.in); String departurePlace; String
		 * terminalPlace;
		 * 
		 * //System.out.print("출발지를 입력: "); departurePlace = input.nextLine();
		 * //System.out.print("도착지를 입력: "); terminalPlace = input.nextLine();
		 * 
		 * int latitude = 3; int longtitude = 5;
		 * 
		 * 
		 * // 받아온 위도와 경도를 이용하여 출발지와 도착지 노드 생성 path.setStartDestination(departurePlace,
		 * latitude, longtitude); path.setArrivalDestination(terminalPlace, latitude,
		 * longtitude);
		 * 
		 * // 첫 번째 여행지의 index = 1, 두 번째 관광지의 index = 2, n번쨰 관광지의 index = n // 알고리즘에 의해
		 * 노드 추가하여 경로 생성
		 * 
		 * // API로부터 목적지 입력시 코드 받아오기
		 * 
		 * 
		 * 
		 * path.addDestination(1, "subway", latitude, longtitude);
		 * System.out.println(path.delete(2)); // 생성된 최단 경로 생성 path.printTravelRoute();
		 * input.close();
		 */
	}

}
